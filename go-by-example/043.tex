\section{Defer}

Defer は、通常クリーンアップなどのために、関数呼び出しが後で実行されることを保証するために使います。 defer は、他のプログラミング言語で ensure や finally が使われる場面でよく使われます。

\lstinputlisting[caption = defer.go]{defer.go}
\lstinputlisting[caption = defer.run,numbers=none]{defer.run}


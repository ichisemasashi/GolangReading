\section{Non-Blocking Channel Operations}

チャネル上の基本的な送受信はブロックします。しかし、\texttt{default} 句をもった \texttt{select} を使えば、ブロックしない (non-blocking) 送信、受信、そして多重 \texttt{select} さえ実装できます。

\lstinputlisting[caption = non-blocking-channel-operations.go]{non-blocking-channel-operations.go}
\lstinputlisting[caption = non-blocking-channel-operations.run,numbers=none]{non-blocking-channel-operations.run}


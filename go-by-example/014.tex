\section{Variadic Functions}

可変長引数関数 (Variadic functions) は、任意個の引数で呼び出すことができます。 例えば、 \texttt{fmt.Println} は一般的な可変長引数関数です。

\lstinputlisting[caption = variadic-functions.go]{variadic-functions.go}
\lstinputlisting[caption = variadic-functions.run,numbers=none]{variadic-functions.run}

Go の関数のさらに別の重要な側面として、 クロージャを作る機能を次回見ていきます。
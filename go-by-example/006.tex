\section{If/Else}

Go の \texttt{if} と \texttt{else} による条件分岐は直感的です。

\lstinputlisting[caption = if-else.go]{if-else.go}
\lstinputlisting[caption = if-else.run,numbers=none]{if-else.run}

Go には 三項演算子 がないため、単純な条件文でも完全な \texttt{if} ステートメントを 使う必要があります。